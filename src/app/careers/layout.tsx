export const metadata = {
  title: "Careers | Corgi Studios",
  description: "Explore open positions at Corgi Studios.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}